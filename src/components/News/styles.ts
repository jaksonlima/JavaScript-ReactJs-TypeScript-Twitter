import styled from "styled-components";

export const Container = styled.div`
    display: flex;
    flex-direction: column;

    > span {
      color: var(--gray);
      margin-bottom: 3px;
    }
`;
